package com.colis.repository;

import com.colis.model.Moyen_transport;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Moyen_transport_Repository extends JpaRepository<Moyen_transport,Long> {
}
